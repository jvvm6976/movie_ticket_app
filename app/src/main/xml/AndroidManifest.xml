<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">

        <!-- SplashActivity là màn hình bắt đầu -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <!-- MainActivity trong package activities là màn hình chính -->
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.MovieListActivity" />
        <activity android:name=".activities.MovieDetailActivity" />
        <activity android:name=".activities.TheaterListActivity" />
        <activity android:name=".activities.ShowtimeListActivity" />
        <activity android:name=".activities.SeatSelectionActivity" />
        <activity android:name=".activities.BookingConfirmActivity" />
        <activity android:name=".activities.MyTicketsActivity" />
        <activity android:name=".activities.ProfileActivity" />

        <service
            android:name=".firebase.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver android:name=".utils.BookingReminderReceiver" android:exported="false" />

    </application>

</manifest>